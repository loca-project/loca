# Loca 要件定義 (4) ランキング関連機能

> 移植元: Loca システム要件定義書 4章。

## 4. ランキング関連機能

### 4.1. 共通仕様
* **適用範囲**: 本セクション（ランキング関連機能）の仕様は、マーカー検索機能には適用しない。
* **デフォルト表示**: フィルタなし、Youtube再生数順（降順）、データは1日1回の定時更新後のものを使用。
* **データ取得ロジック**:
    * **フィルタなし**: 定時更新された基本データ（最大100件）はサーバー側でキャッシュ済み。
    * **フィルタ適用時**: サーバー負荷回避のため、クライアントサイドでキャッシュ済み基本データ（最大100件）に対して再集計を行う。
    * **ページネーション**: 100件以上のデータを見る場合は、サーバーへ新規リクエストを行い、100件ずつ表示する。
* **フィルタ適用時の制限**:
    * フィルタ適用中に表示件数が100件未満の場合、ページネーション（「もっと見る」等）は非表示/無効化する。
    * 以下のメッセージを一覧下部に表示する：「現在、フィルタが適用されているため、表示は最大100件に制限されています」
* **表示形式**: ~~Youtube URLは列幅節約のため、すべて **"Link"** というハイパーリンクで表示する。~~（2026-09-25 に廃止。動画はマーカー情報から開く）
* **結果表示エリア**:
    * 各種ランキングの集計結果は、地図上の**「共通結果パネル（オーバーレイ）」**に表示する。
    * サイドメニューの開閉に連動して表示位置が移動し、右上の「閉じるボタン」で非表示化可能。

### 4.2. 共通フィルタ項目
* **UI配置**: 共通フィルタと個別フィルタは、スマートフォンでもタップしやすいように**縦一列**で配置する。
* **実行アクション**:
    * フィルタ項目の変更だけでは集計を実行しない。
    * 項目群の最下部に配置する**「適用」ボタン**（または「検索」ボタン）を押下したタイミングでフィルタリングを実行し、結果パネルを表示・更新する。
    * **UI挙動（モバイル）**: スマートフォン（モバイルビュー）での操作時、**適用ボタンを押下したタイミングでサイドメニューを自動的に閉じる（非表示にする）**。
* **動画投稿日**: 全期間, 1年以内, 半年以内, 3ヶ月以内, 1ヶ月以内, 2週間以内, 当日
* **動画の長さ**: すべて, 4分未満, 4分以上20分未満, 20分以上
* **タグ（複数選択可。ADR 0014・0015）**:
    * **UI配置**: **動画の長さ**フィルタの下に、次の 5 項目を縦一列で並べる。語は要件 3.3 の表と同じ。
    * **選択形式**: 複数選択が可能な**チェックボックス形式**（またはチップ）で実装する。
    * **フィルタロジック**: **同じ項目の中は OR、項目の間は AND**。地図フィルタ（要件 2 章・ADR 0015）と同じ規則にする。
      例: 雰囲気「幻想的・壮大」＋時間帯「夜」は、「（幻想的 または 壮大）かつ 夜」。
    * 任意項目（季節・時間帯・撮り方）が未設定のマーカーは、その項目で絞ったときは当たらない。

| UI配置順 | 項目 | 選択肢 |
|---|---|---|
| **1** | 映っているもの | 自然・景色, 街並み・建物, 祭り・イベント, 食, 乗り物・交通, 史跡・文化, 暮らし・人 |
| **2** | 雰囲気 | 賑やか, 穏やか, 幻想的, 壮大, 懐かしい, スリル（マーカーと同じ色の印を付ける） |
| **3** | 撮影の季節 | 春, 夏, 秋, 冬 |
| **4** | 撮影の時間帯 | 日の出, 朝, 昼, 夕方, 夜 |
| **5** | 撮り方 | 空撮, 歩き撮り, 車載・乗車, 定点 |

* **撮影リクエストランキング**: タグは撮影の季節・時間帯・撮り方の 3 項目だけを出す。絞ると、条件に合うリクエストの熱量だけで数え直して並べる（ADR 0014）。

> 移植元の「感情タグ」3 カテゴリ（行動への影響・動画の雰囲気・感情の核。全タグを OR）は 2026-09-24 に廃止した（ADR 0014）。

* **表示件数**: 30, 20, 10
* ※クライアント保持の最大100件に対し、さらに絞り込むUI機能として動作させる。

### 4.3. 個別ランキング仕様
* **地域別ランキング**:
    * 都道府県フィルタ: プルダウン選択、登録された都道府県情報をもとに集計。
    * 季節フィルタ: Youtube投稿日をもとに集計（1月-3月, 4月-6月, 7月-9月, 10月-12月）。
* **チャネルランキング**:
    * チャンネル名が同じマーカーの動画件数と再生数を合算し、再生数順で表示。
* **機器別ランキング**:
    * **フィルタ項目**: **分類**、**メーカー**、**シリーズ**、**モデル**の 4 段のプルダウン（2026-09-24 改訂。ADR 0018）。上の段を選ぶと下の段にはその中身だけが出る。
    * **フィルタ実行の条件**:
        * **いずれかの項目が選択されていれば**、その条件でフィルタリングを実行する。
        * **未選択の項目**はフィルタ対象外（すべての値を含む）として扱うことで、単一項目だけのフィルタリング（例: メーカーのみ）を可能とする。
    * **UI連動制御**:
        * **初期状態**: すべてのプルダウンは「すべて」または「未選択」で初期化される。
        * **連動とグレーアウト**:
            * **メーカー**が「すべて」/「未選択」の場合、**シリーズ**と**モデル**のプルダウンは**グレーアウト（無効化）**する。
            * **メーカー**が選択されると、**シリーズ**のプルダウンには、そのメーカーに紐づく値のみが表示される。
            * **シリーズ**が選択されると、**モデル**のプルダウンには、そのメーカーとシリーズに紐づく値のみが表示される。
    * **デザイン**: **青色の強調デザイン**を適用し、タップしやすい縦一列の配置とする。
    * **表示順**: 登録情報をもとにフィルタし、再生数順で表示する。

### 4.4. ランキング結果からの地図ジャンプ機能

ランキングタブ（地域別、チャネル、機器別）のフィルタ実行後に表示される**共通結果パネル**内のレコード一覧に対し、以下の地図ジャンプ機能を実装する（`3.6.` の選択とジャンプボタンは使わず、行ごとのボタンにする）。

* **UI要素**（2026-09-25 改訂）:
    * 一覧の各レコードの行内に、**「地図へ」ボタン**を配置する。マーカーの行には、同じ大きさの**「共有」ボタン**をその右に並べる（動線の順）。
    * 行のチェックボックスと、結果パネル下部の一括の「地図へジャンプ」ボタンは**置かない**。複数選んだときにどこへ飛ぶのか分からないため（検索・範囲指定・ランキングの結果すべて）。
* **行の「地図へ」ボタン**:
    * **トリガー**: 各レコード行の**「地図へ」ボタン**を押したとき。
    * **アクション**:
        1.  地図画面に遷移し、選択されたレコードに対応するマーカーの位置が、地図の中心となるように移動（パン）させる。
        2.  マーカーが明確に視認できる**適切な縮尺サイズ**（例：ズームレベル14～16程度）に自動的に調整（ズーム）する。

---


---

## Loca での扱い（2026-09-22 の構成確定による変更）

Google API を使わない決定により、**再生数・動画投稿日・再生時間は取得できない**
（YouTube oEmbed が返すのは タイトル / チャンネル / サムネイル のみ）。
本章の基準を Loca 内の指標に置き換えた。判断の経緯は
[0006-ランキング基準をLoca内の指標に変える.md](../decisions/0006-ランキング基準をLoca内の指標に変える.md)。

> **2026-09-24 追記（T25・ADR 0017）**: 再生数・動画の投稿日・長さは、毎晩 Actions が YouTube Data API で取るようになった。
> 下の表の置き換えは元の要件に戻した。まだ取っていないマーカー（投稿した当日）は、再生数 0・投稿日は登録日で代わりに数え、
> 長さで絞ったときは当たらない。

| 本章の記述 | 現在の実装 |
|---|---|
| 4.1 再生数順（降順） | 要件どおり（再生数の合計、同じなら件数、さらに新しい順） |
| 4.2 動画投稿日フィルタ | 要件どおり（投稿日が未取得なら登録日） |
| 4.2 動画の長さフィルタ | 要件どおり（4 分未満・4〜20 分・20 分以上） |
| 4.3 地域別＝再生数順 | 要件どおり。季節フィルタも動画の投稿日の四半期 |
| 4.3 チャネル＝再生数合算 | 要件どおり（本数と再生数を合算し、再生数順） |
| 4.3 機器別＝再生数順 | 要件どおり |
| 4.3 季節フィルタ（投稿日） | 要件どおり（動画の投稿日の四半期） |
| 4.1 100 件上限・ページネーション | 上限のみ維持。ページネーションは未実装 |

タグフィルタ（ADR 0014 で項目を作り直し、項目の間を AND に変更）、表示件数（30/20/10）、共通結果パネル、
地図ジャンプ（4.4。行ごとの「地図へ」だけ。2026-09-25 に選択と一括ジャンプを廃止）は維持している。
