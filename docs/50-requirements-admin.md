# Loca 要件定義 (5) 管理者モード

> 移植元: Loca システム要件定義書 5章。

## 5. 管理者モード関連機能 (新規追加・変更)

### 5.1. 管理者モードへのアクセス
* **トリガー**: Google認証アイコンクリック時のポップアップに表示される「**管理者モード**」ボタンを押下。
* **画面表示**: 画面中央に管理者モード専用の画面をオーバーレイで表示する。
* **画面構成**:
    * 画面上部に**タブ形式のナビゲーション**を配置し、各機能画面を切り替えられるようにする。
    * 各機能画面は、タブをクリックした際に切り替わり、他の画面のデータは非表示となる。
    * タブの配置順は以下の通りとする（2026-09-25 改訂。データエクスポートのタブは廃止し、書き出しは投稿動画・撮影リクエストのタブで行う）。
        1.  📊 統計分析（Statistics）
        2.  🛠️ **ユーザー管理**
        3.  🎬 **投稿動画**（旧「マーカー管理」）
        4.  ✋ **撮影リクエスト**（機器管理（T28）はこの右に置く予定）
        5.  🕒 定期処理
        6.  🚩 通報
        7.  📜 ログ一覧（Marker Logs）

### 5.2. 管理者モードの機能タブ

#### 5.2.1. 📊 統計分析（Statistics）
* **機能**: Youtubeの投稿日をもとに期間を指定し、マーカーの登録情報の統計を表示する。
* **表示対象**: **タグ**（映っているもの、雰囲気、撮影の季節、撮影の時間帯、撮り方。要件 3.3）、**撮影機器**（メーカー、シリーズ、モデル）などの登録されたマーカーの統計情報。
* **期間指定**: 統計を集計する期間（例：〇年〇月～〇年〇月）を指定するためのUI（日付ピッカーなど）を提供する。
* **表示形式**: 統計情報は、グラフ（例：円グラフ、棒グラフ）や集計表（件数）などで視覚的に分かりやすく表示する。

#### 5.2.2. 📤 データエクスポート（Data Export）
* **機能**: 登録されているマーカー情報の一部をデータセットとしてエクスポートする。
* **エクスポート対象データ**:
    * マーカーの**Youtube URL**
    * **タグ**（映っているもの、雰囲気、撮影の季節、撮影の時間帯、撮り方）。値は要件 3.3 のキーで出す
    * **現地メモ**
* **エクスポート形式**: CSVまたはJSON形式でのファイルダウンロード機能とする。
* **データ範囲**: すべての登録済みマーカーデータ（論理削除されたものは除く）を対象とする。

#### 5.2.3. 📜 ログ一覧（Marker Logs）
* **機能**: マーカーの登録および更新に関するログを一覧表示する。
* **表示項目**:
    * マーカーの**登録日**
    * マーカーの**編集などの更新日**
    * その他、ログとして有用な情報（例：マーカーID、操作ユーザーIDなど）。
* **表示形式**: 日時順（降順）のテーブル形式で表示し、必要に応じてソートやフィルタリング機能を実装する。

#### 5.2.4. 🛠️ ユーザー管理（User Management）
* **機能**: サイトに登録されたユーザー情報の管理およびアクセス制御を行う。
* **データベース同期**: ユーザー情報と**Firestore**のデータは常に**同期が取れた状態**を保証する。
* **最新情報取得のための強制同期ボタン**: 同期エラー時などに備え、「ユーザー管理」タブ内に**「ユーザー情報の取得」ボタン**を実装する。ボタン押下で**Firestoreから強制的に最新情報を取得**し、画面表示およびプロフィールを管理する関連ファイルも最新の状態に更新する。
* **画面構成**: 以下の4つのセクションを**横並び**で**一画面**に表示する。すべてのセクションで**Google IDとGoogle表示名**は**必須かつ併記**して表示する。

| セクション名 | 対象ユーザー | Google ID | Google表示名 | その他の表示項目 | 操作ボタン |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **管理者ユーザー** | 管理者権限を持つユーザー | 必須 | 必須 | - | - |
| **現在のユーザー一覧** | サイトに登録済みの一般ユーザー | 必須 | 必須 | **登録日** | 手動での**削除**、**要注意ユーザー**への移動、**ブラックリスト**への移動 |
| **要注意ユーザー** | 「現在のユーザー一覧」から手動で**削除された履歴**を持つユーザー | 必須 | 必須 | - | **登録解除**（リストから削除） |
| **ブラックリスト** | 「要注意ユーザー」から**指定・登録**されたユーザー | 必須 | 必須 | - | **登録解除**（リストから削除） |

* **ブラックリスト機能の挙動**:
    * 「ブラックリスト」に登録されているユーザーがGoogle認証を試みた場合、**プロフィール登録画面を表示する前**にログインを不可とする。
    * ユーザーに対して「登録が制御されています」という旨の**ポップアップメッセージ**を表示する。
* **ユーザー削除時の挙動**: ユーザーの削除が行われた場合、**Firestoreからも該当データが完全に削除**されるように実装する。

#### 5.2.5. 🛠️ マーカー管理（Marker Management）
* **機能**: 特定のGoogleアカウントに紐づくマーカーの一覧表示、選択削除、地図へのジャンプ機能を提供する。（旧 `5.2.4. ユーザー別管理` からマーカー管理機能を分離）
* **ユーザー指定**: **Googleアカウント（メールアドレスなど）を指定**するための検索/入力フィールドを画面上部に配置する。
* **一覧表示**: 指定されたGoogleアカウントが登録した**すべてのマーカー情報**を一覧で表示する。
* **操作機能**:
    * **選択形式**: マーカー一覧に**チェックボックス**を配置し、複数選択を可能とする。
    * **削除機能**: 選択されたマーカーを一括で削除するボタンを配置する。この削除は、`1.4. マーカーデータ読み込みのリアルタイム・ハイブリッド化`で定義された**論理削除（Soft Delete）**として実行される。
    * **地図へのジャンプ機能**:
        * **単一選択時**: 選択されているマーカーが**1つのみ**の場合、**「地図へジャンプ」ボタン**を**活性化**する。ボタンを押下すると、そのマーカーを地図の中心に表示し、適切な縮尺にズームする（`3.6.` に準拠）。
        * **複数選択時・未選択時**: 選択されているマーカーが**2つ以上**または**0件**の場合、**「地図へジャンプ」ボタン**は**非活性化（グレーアウト）**する。
---

## Loca での扱い（移植時の差分）

2026-09-25 時点の管理者モード（`src/features/admin/AdminDashboard.tsx`）は、次の 7 タブをこの順に並べる（5.1 の改訂）。
各タブの中身は下の追記（T27〜T61）にある。

| タブ | 要件 | 状態 |
|---|---|---|
| 統計分析 | 5.2.1 | 実装済み（T27） |
| ユーザー管理 | 5.2.4 | 実装済み（管理者・一般・ブラックリストの 3 区画。T59） |
| 投稿動画 | 5.2.5・5.2.2 | 実装済み（T60。2026-09-25 に「自分の投稿」と同じ形に改訂） |
| 撮影リクエスト | 5.2.5 に準じる・5.2.2 | 実装済み（2026-09-25。管理者の取り下げは持ち主の熱量を戻す） |
| 定期処理 | 追加（T24 から移管） | 実装済み（T27） |
| 通報 | 3.8 の受け皿 | 実装済み（T61） |
| ログ一覧 | 5.2.3 | 実装済み（T60） |

データエクスポート（5.2.2）のタブは 2026-09-25 に廃止した。書き出しは投稿動画・撮影リクエストのタブで、
選んだものを CSV / JSON にする（左の列の「すべての投稿者」ですべて選べば全件）。

### 投稿動画・撮影リクエストのタブ（2026-09-25）

「自分の投稿」の同じ名前のタブと同じ形にし、左に「ニックネームで探す」の列を足した。

| 部分 | 内容 |
|---|---|
| 左の列 | ニックネームで探す欄と、合う投稿者の一覧（件数の多い順）。先頭の「すべての投稿者」は、探した名前に合う人の全員を対象にする |
| 上の操作 | すべて選択・選んだものを削除（取り下げ）・CSV / JSON でエクスポート（`src/features/profile/PostsToolbar.tsx` を共有） |
| 行 | 題名と補足（投稿者・場所・日付）、「地図へ」と「削除」（撮影リクエストは「取り下げ」）。撮影リクエストは 1 行が 1 地点 |
| 撮影リクエストのニックネーム | リクエストは uid だけを持つので、マーカーの投稿者名、なければプロフィール（`users`）から引く。どちらにも無ければ「（ニックネーム不明）」と uid の先頭 |
| 管理者の取り下げ | 論理削除（`withdrawn`）。持ち主の熱量の印（`heatBudgets/{ownerUid}`）を同じバッチで減らすので、熱量はその分戻る。ルールは `rules/20-requests.rules`、テストは `tests/rules/requests.test.mjs`・`tests/store/adminStore.test.mjs` |

移植元にあった次のタブは作っていない。

| タブ | 扱い |
|---|---|
| 機器マスタ | 未実装。元データ `scripts/data/equipment-master.mjs` を編集して `npm run data:equipment` で作る（ADR 0018）。画面での編集は T28 |
| アクセス制限（IP の台帳） | 作らない。静的配信では IP を判定できない（10-requirements-common「未実装として明示するもの」） |
| バックアップ | 作らない。消したものは 30 日間論理削除のまま残る（ADR 0021） |

- **データの経路**: 管理者モードは `AdminStorePort`（`src/adapters/firebase/adminStore.ts`）越しに Firestore を読み書きする。権限はルールが守る。
- **ブラックリストの挙動**: ログイン直後、プロフィールを読んだときにブラックリストなら、登録画面を出す前にログアウトさせて案内を出す
  （`src/features/profile/ProfileGate.tsx`。T59）。
- **管理者の判定**: Firestore の `admins/{uid}` があるかどうか。登録は `npm run admin:add`。メールアドレスでは判定しない（ADR 0019）。

## 追記（2026-09-24 統計とエクスポートの置き場所）

統計分析（5.2.1）とデータエクスポート（5.2.2）は、ログインを持たなかった間（2026-09-22〜）は誰でも開ける公開の機能だった。
ログインとプロフィール（ADR 0019）ができたので、次のように分けた（利用者の判断）。

| 対象 | 置き場所 | 状態 |
|---|---|---|
| サイト全体の統計とエクスポート | 管理者モード（`src/features/admin/AdminDashboard.tsx`） | 公開の入口を外した。管理者画面（T27）から開く |
| 自分の投稿だけの統計とエクスポート | 右上のメニューの「自分の投稿」（タブ: 統計・投稿動画・撮影リクエスト）。統計には受け取ったいいねの合計、投稿動画の行には件数も出す（0 件は出さない。T29）。投稿動画と撮影リクエストは行ごとに地図へ・削除（取り下げ）、チェック（すべて選択あり）で選んだものを一括削除・CSV / JSON で書き出し | 実装済み（T53。2026-09-24 改訂） |

2 つの画面はタブの部品（`src/shared/components/DashboardTabs.tsx`）と統計・エクスポートのタブを共有し、同じ形にする。

## 追記（2026-09-24 管理者モードの入口。T27）

| 要件 | 実装 | 状態 |
|---|---|---|
| 5.1 管理者モードへのアクセス | 右上のメニューの「プロフィール」の下に「管理者モード」。`admins/{uid}` がある人だけに出す（表示の切り替え。権限はルール） | 実装済み |
| 5.2.1・5.2.2 統計分析・データエクスポート | 統計は管理者モードのタブ（サイト全体）。書き出しは投稿動画・撮影リクエストのタブ（2026-09-25 にエクスポートのタブを廃止） | 実装済み |
| 定期処理（T24 から移管） | `jobs/{id}`（YouTube の更新・30 日後の物理削除）の最後の実行を出す | 実装済み |
| 5.2.4 ユーザー管理 | 管理者・一般のユーザー・ブラックリストの 3 区画（ニックネームと uid）。操作は「削除」（プロフィールと名前の索引。マーカーは消さない）と「ブラックリスト」。要注意の一覧は作らない | 実装済み（T59） |
| 5.2.4 ブラックリストの挙動 | ログイン直後、登録画面を出す前に案内を出してログアウトさせる。書き込みはルールが拒否する | 実装済み（T59） |
| 5.2.3 ログ一覧 | マーカーを更新の新しい順に表で出す（更新日時・登録日時・題名・投稿者・マーカー ID・uid。200 件まで・絞り込みあり）。論理削除したものは出ない | 実装済み（T60） |
| 5.2.5 マーカー管理（タブ名は「投稿動画」） | メールの代わりにニックネームで投稿者を探し、マーカーを選んで一括で論理削除（ADR 0021）。地図へは行ごとのボタン（1 件だけ選んだときの一括ジャンプは 2026-09-25 に廃止） | 実装済み（T60） |
| 通報 | マーカーごとに人数・確認待ち・理由の内訳・詳細と、対象の題名・投稿者・現地メモ。操作は地図へ・マーカーを削除（論理削除して対応済み）・対応済みにする | 実装済み（T61） |

利用者はニックネームと uid で見分ける（メールアドレスと Google の名前は保存しない。ADR 0019）。
要件 5.2.4 の「Google ID と Google 表示名」と 5.2.5 の「メールアドレスの指定」は、それぞれ uid とニックネームに置き換える。
